#include <iostream>

using namespace std;
class passenger{
private:


public:
    passenger();
    passenger(const string& nm, mealtype mp, const string };

int main(){
{

    return 0;}
    /*an imp aspect of classes is the capability to initialise the classes member data.
    constructor is a special member fxn whose task is to perform such as initialisation.
    it is invoked when a new class object is existance .ther is an analogous destructor fxn that is called when a class goes out of existance
    a constructor member fxn name is same as the class and it has no return type.
    */


}
